import React, { useState } from 'react';
import Result from '../Result';

export default function ExamCardList() {
  const [exams, setExams] = useState([]);

  // Assume exams state is populated somehow

  return (
    <Result></Result>
  );
}
